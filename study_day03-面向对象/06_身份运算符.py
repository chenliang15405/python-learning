"""
身份运算符用于比较两个对象的内存地址是否一致，是否是同一个对象的引用

有两种：
    is      用于判断是否是同一个对象引用，     x is y 类似于 id(x) == id(y)
    is not   用于判断是否是不同对象的引用      x is not y 类似于 id(x) != id(y)


is 与 ==区别：

    is 用于判断两个变量引用对象是否为同一个
    == 判断引用对象的值是否相等



"""