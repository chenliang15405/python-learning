"""
操作文件或者目录：

    1. 先导入 import os

    2. 调用模块的方法操作

    文件操作：
        os.rename(源文件名, 目标文件名) 重名名
        os.remove(文件名) 删除文件

    目录操作：
        os.listdir(目录列表)  目录列表
        os.mkdir(目录名)   创建目录
        os.rmdir(目录名)   删除目录
        os.getcwd()     获取当前目录
        os.chdir(目标目录)  修改工作目录
        os.path.isdir(文件路径) 判断是否是文件


"""